# services/ocr — validado por app/config.py (pydantic-settings).
NODE_ENV=production
PORT=8001

INTERNAL_API_TOKEN=
OCR_DEFAULT_PROVIDER=paddle

# Solo si se activa un provider cloud
AWS_REGION=
GOOGLE_CREDENTIALS_JSON=

LOG_LEVEL=info
SENTRY_DSN=
